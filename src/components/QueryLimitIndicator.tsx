@@ .. @@
       <span>
         {canQuery 
-          ? `${remainingQueries} queries remaining today`
-          : 'Daily limit reached! Try again tomorrow.'
+          ? `${remainingQueries} queries remaining today 🎯`
+          : 'Daily limit reached! Try again tomorrow 🙏'
         }
       </span>